<UserControl x:Class="UserControlTxtNum"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PicSorterNS"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Loaded="UserControl_Loaded">
        <StackPanel>
            <CheckBox IsChecked="{Binding AlsoEmpty, Mode=TwoWay}" x:Name="uiCheckBox" />
        <local:UserControlPolaTxt0123 DataContext="{Binding FldTxt, Mode=TwoWay}" />
            <local:UserControlPolaNum0123 DataContext="{Binding FldNum, Mode=TwoWay}" x:Name="uiNumFlds"/>
        </StackPanel>
</UserControl>
